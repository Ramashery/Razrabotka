# Файл: netlify.toml

# Эта функция будет применяться ко всему сайту...
[[edge_functions]]
  path = "/*"
  function = "prerender"
  
  # ...КРОМЕ этого конкретного пути.
  # Это гарантирует, что Netlify отдаст статический файл robots.txt как есть.
  excluded_patterns = [
      "^/robots\\.txt$"
  ]