{
  "hosting": {
    "site": "razrabotka-b61bc",
    "public": "public",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    
    // --- ДОБАВЛЕНО: Правила для красивых SEO-ссылок ---
    "cleanUrls": true,  // Позволяет обращаться к /page вместо /page.html
    "trailingSlash": true, // Автоматически добавляет / в конец URL, чтобы /page стал /page/

    // --- ДОБАВЛЕНО: Правила переадресации ---
    // Это самое важное правило для гибридных сайтов.
    // Оно говорит Firebase: "Сначала попробуй найти реальный файл или папку.
    // И только если ничего не найдено, отдай /index.html для работы JavaScript-роутера".
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],

    // Ваш блок с заголовками кеширования остается без изменений, он настроен хорошо.
    "headers": [
      {
        "source": "**/*.@(js|css|svg|png|jpg|webp|woff|woff2)",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "public, max-age=31536000, immutable"
          }
        ]
      },
      {
        "source": "**/*.html",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "public, max-age=0, must-revalidate"
          }
        ]
      }
    ]
  }
}
